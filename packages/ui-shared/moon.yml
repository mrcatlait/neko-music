$schema: 'https://moonrepo.dev/schemas/project.json'

stack: 'frontend'
type: 'library'
language: 'typescript'

tasks:
  build:
    command:
      - 'ng-packagr'
      - '-p ng-package.json'
