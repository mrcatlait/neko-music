$schema: 'https://moonrepo.dev/schemas/project.json'

platform: 'bun'
type: 'library'
language: 'typescript'

dependsOn:
  - id: 'eslint-config'
    scope: 'development'

tasks:
  build:
    command: 'tsc'
    # inputs:
    #   - '@globs(sources)'
    outputs:
      - 'lib'