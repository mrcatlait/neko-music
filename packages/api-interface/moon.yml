$schema: 'https://moonrepo.dev/schemas/project.json'

stack: 'frontend'
layer: 'library'
language: 'typescript'

dependsOn:
  - id: 'eslint-config'
    scope: 'development'

tasks:
  codegen:
    command: 'graphql-codegen'
    args:
      - '--config'
      - 'codegen.ts'
    inputs:
      - 'codegen.ts'
