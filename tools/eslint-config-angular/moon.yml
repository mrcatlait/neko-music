$schema: 'https://moonrepo.dev/schemas/project.json'

stack: 'frontend'
type: 'library'
language: 'javascript'

dependsOn:
  - id: 'eslint-config'
    scope: 'production'

toolchain:
  typescript:
    disabled: true
