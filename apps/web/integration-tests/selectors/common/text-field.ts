import { Selectors } from '../../types'

export const textFieldSelectors = {
  fieldLabel: 'text-field-label',
  fieldInput: 'text-field-input',
  errorLabel: 'text-field-error-label',
} satisfies Selectors
