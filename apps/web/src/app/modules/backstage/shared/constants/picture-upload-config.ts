export const PICTURE_UPLOAD_CONFIG = {
  /**
   * The maximum file size in bytes (10MB)
   */
  maxFileSize: 10 * 1024 * 1024,
  /**
   * The allowed file types.
   */
  allowedTypes: ['image/jpeg', 'image/jpg', 'image/png', 'image/webp'],
}
